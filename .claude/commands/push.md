---
description: Give me a `git` command line to make the initial push of the current branch. Echo the command and pipe it to clip.exe
---

Give me a `git` command line to make the initial push of the current branch. Echo the command and pipe it to clip.exe