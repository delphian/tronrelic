# TronRelic Environment Configuration
# Works for both Docker Compose and local development (./scripts/start.sh)

# ==============================================
# Backend Configuration
# ==============================================
# ENV: Deployment environment (development, staging, production)
ENV=development

# ==============================================
# Site Configuration
# ==============================================
# SITE_URL: Where the frontend is accessible
# SITE_WS: Where WebSocket connections go (Socket.IO backend)
# SITE_BACKEND: Where frontend can reach backend for SSR (Server-Side Rendering)
SITE_URL=http://localhost:3000
SITE_WS=http://localhost:4000
SITE_BACKEND=http://localhost:4000
PORT=4000
ENABLE_SCHEDULER=true
ENABLE_WEBSOCKETS=true

# Database (Docker uses service names, local uses localhost)
# Docker Compose will override these via docker-compose.yml environment section
MONGODB_URI=mongodb://127.0.0.1:27017/tronrelic
REDIS_URL=redis://127.0.0.1:6379
REDIS_NAMESPACE=tronrelic

# ==============================================
# Production Only (Leave blank for development)
# ==============================================
# Generate with: openssl rand -hex 32 (for each)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=
REDIS_PASSWORD=

# ==============================================
# Required - API Security
# ==============================================
# Generate with: openssl rand -hex 32
ADMIN_API_TOKEN=

# ==============================================
# Required - TronGrid API Keys
# ==============================================
# Get from https://www.trongrid.io/
TRONGRID_API_KEY=
TRONGRID_API_KEY_2=
TRONGRID_API_KEY_3=

# ==============================================
# Droplet Deployment Configuration
# ==============================================
# Required for deployment scripts (./scripts/droplet-*.sh)
# Leave blank if you're only doing local development
# Production droplet IP address
PROD_DROPLET_IP=
# Development droplet IP address
DEV_DROPLET_IP=
# GitHub Container Registry credentials
GITHUB_USERNAME=
GITHUB_REPO=
