/**
 * UI State Feature Module
 *
 * This module handles global UI state including:
 * - Modal state
 * - Toast notifications
 * - Loading states
 * - UI preferences
 */

// Redux slice
export { default as uiReducer } from './slice';
export * from './slice';
