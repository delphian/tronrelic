/**
 * @fileoverview Public surface of the frontend curation module — the admin API
 * client for the central curation queue. Consumers import from the module root.
 */

export * from './api/client';
