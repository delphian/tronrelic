/**
 * Admin components for the `/system/users` identity dashboard.
 *
 * Account directory and group management. Traffic analytics panels moved
 * to `modules/traffic/components/admin` with the identity/traffic split.
 */

export { UsersMonitor } from './UsersMonitor';
export { GroupsManager } from './GroupsManager';
