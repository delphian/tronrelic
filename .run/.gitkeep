# This directory stores runtime logs for local development
# It is automatically created by scripts/start.sh and mounted in Docker Compose
# Git tracks this .gitkeep to ensure the directory exists with proper ownership
